{
  "version": 2,
  "buildCommand": "npm install && npx prisma generate && npm run build && npx esbuild api/index.js --bundle --platform=node --outfile=api/b.js --external:@prisma/client --external:.prisma/client && mv api/b.js api/index.js",
  "installCommand": "npm install",
  "functions": {
    "api/index.js": {
      "memory": 512,
      "maxDuration": 30,
      "includeFiles": "node_modules/.prisma/**,node_modules/@prisma/client/**"
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index.js"
    }
  ]
}
