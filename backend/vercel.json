{
  "version": 2,
  "buildCommand": "npm install && npx prisma migrate resolve --applied 20260429000000_add_societes_intervenants; npx prisma migrate resolve --applied 20260505000001_role_string_manager_zone; npx prisma migrate resolve --applied 20260731000001_correction_inventaire; npx prisma migrate deploy || true; npx prisma generate && npm run build",
  "installCommand": "npm install",
  "functions": {
    "api/index.js": {
      "memory": 512,
      "maxDuration": 30,
      "includeFiles": "dist/**,node_modules/.prisma/**,node_modules/@prisma/client/**"
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index.js"
    }
  ]
}
